# credentials/broken
$ issue show DEMO-0001
env LOOPTRACK_TOKEN=

#### exit 1

#### stdout

#### stderr
エラー: $TMP/home/.config/looptrack/credentials.json を読めません（JSON の形式が壊れています）。削除して looptrack issue login --browser をやり直してください

#### requests (0)

#### files (1)
home/.config/looptrack/credentials.json (0600)
    {"broken
    (末尾に改行なし)
