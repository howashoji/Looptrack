# init/server-codex-copilot
$ issue init --project demo --source server --agent codex,copilot --no-loop

#### exit 0

#### stdout
init: $TMP/ws（プロジェクト demo・$API・AI codex, copilot）
kit の取得元: サーバ（$API/api/v1/dist・SHA-256 を確認）
配線: looptrack hook <名前> --agent <AI>
導入セット: core のみ（loop は辞退を記録。入れるなら looptrack issue init --loop）
  作成         .codex/hooks.json
  作成         .codex/config.toml
  作成         .github/hooks/looptrack.json
  作成         AGENTS.md
  作成         .claude/.looptrack-kit.json
verify: 配線 8 件・looptrack <版> を確かめました
次に:
  1. Codex のフックを信頼する（下の「フックの信頼」。ターミナルの codex で /hooks）
  2. Copilot: 新しいチャット（VS Code）か新しいセッション（Copilot CLI。このフォルダを信頼する）を始める（.github/hooks/looptrack.json のフックが読み込まれる）
  3. 確認（Codex）: 新しいセッションで MCP の setup が「導入済み」を返し、guide → next（dry_run）が通る。CLI は MCP に無い操作だけ（AGENTS.md）
  4. 確認（Copilot）: 新しいセッションで MCP の setup が「導入済み」を返し、guide → next（dry_run）が通る。CLI は MCP に無い操作だけ（AGENTS.md）
Codex の設定:

  - AI の操作は MCP のツールが主（AGENTS.md の案内）。MCP の接続は ~/.codex/config.toml に利用者が足す（init は書かない）:

      [mcp_servers.looptrack]
      url = "$API/mcp"
      http_headers = { "X-Looptrack-Project" = "demo" }
      # 認証はブラウザでの許可（codex mcp login looptrack）

  - .codex/config.toml（プロジェクト）の [shell_environment_policy] の set に LOOPTRACK_API_URL / LOOPTRACK_PROJECT を init が書いた
    （MCP に無い操作で AI が打つ looptrack issue 用。サンドボックスのネットワークの許可は書かない。CLI は実行のたびに利用者の承認が要る）
  - フックの信頼: Codex のフックは、プロジェクトが信頼済み（trusted）で、かつフックごとに信頼したものだけが動く（未信頼・変わったフックは知らせなしに飛ばされる）。ターミナルの Codex CLI（codex）をこのディレクトリで起動し、/hooks で .codex/hooks.json のフックを確認して信頼する。信頼は ~/.codex/config.toml の [hooks.state] に記録され、デスクトップ版にも効く（デスクトップ版のチャット欄の /hooks はコマンドにならない）。その後に始めたセッションから動く
  - init がフックの配線を変えたとき（再実行で「更新 .codex/hooks.json」）も、変わったフックを同じ手順で信頼し直す
  - 旧来の notify はフックで置き換えた（既存の notify 設定は変えない。フックの無い古い Codex は更新する）
  - 鮮度ガード（Stop で止める）は Claude Code だけに配線する
GitHub Copilot の設定（VS Code のエージェントモード・Copilot CLI。Copilot CLI は 2026-09-19 に 1.0.86 の実物で確かめた。VS Code は公式文書で確かめた範囲で、実物では未確認）:

  - AI の操作は MCP のツールが主（AGENTS.md の案内）。MCP の接続は利用者が足す（init は --mcp のときだけ .vscode/mcp.json を書く）:
      VS Code（.vscode/mcp.json。初回の接続でブラウザの許可）:
        {"servers": {"looptrack": {"type": "http", "url": "$API/mcp", "headers": {"X-Looptrack-Project": "demo"}}}}
      Copilot CLI（リポジトリの .github/mcp.json か .mcp.json、または ~/.copilot/mcp-config.json。tools は必須。
      1.0.22 から .vscode/mcp.json は読まない。init は --mcp のときだけ .github/mcp.json を書く。認証は copilot の /mcp auth looptrack）:
        {"mcpServers": {"looptrack": {"type": "http", "url": "$API/mcp", "headers": {"X-Looptrack-Project": "demo"}, "tools": ["*"]}}}
    Copilot CLI の MCP のツール呼び出しは毎回承認が要る。事前に許すなら copilot --allow-tool='looptrack' で起動する
  - フック（.github/hooks/looptrack.json）: VS Code は .github/hooks/*.json を既定で読む（Preview の機能。組織の設定で無効のことがある）。
    Copilot CLI は起動時にこのフォルダを信頼したとき（「今後も信頼」）だけ読む。どちらも新しいセッションから動く。
    出力は CLI（トップレベル）と VS Code（hookSpecificOutput）の両方の形で出す。VS Code は matcher を無視するので、hook がツール名を見て絞る
  - Windows: hook は looptrack（PowerShell の powershell / windows のフィールドにも同じ looptrack hook を置いた。Windows の実物では未確認）。
    導入済みにならなければ looptrack issue installed --agent copilot で知らせる
  - トークン計測のフック（looptrack hook usage --agent copilot。PostToolUse・Stop・SessionEnd）を入れた。OpenTelemetry のファイル出力を有効にすると計測される
    （出力先は ${COPILOT_HOME:-~/.copilot}/otel/ の下に置く。Copilot CLI は出力先を hook に渡さない。手順は docs/AI-GUIDE.md §7-3-1）。有効にしていなければ何も送らず、Copilot の操作は未付与に数えない
  - 鮮度ガード（Stop で止める）は Claude Code だけに配線する
  - AI が打つ CLI には LOOPTRACK_API_URL / LOOPTRACK_PROJECT が渡らない（Copilot に環境変数の設定が無い）。AGENTS.md で前置を案内した

#### stderr

#### requests (2)
GET /im/api/v1/dist
  Authorization: Bearer imp_golden_token
  X-Looptrack-Client: cli
  X-Looptrack-Lang: ja
GET /im/api/v1/dist/kit/core/skills/issue/SKILL.md
  Authorization: Bearer imp_golden_token
  X-Looptrack-Client: cli
  X-Looptrack-Lang: ja

#### files (5)
ws/.claude/.looptrack-kit.json (0644)
    {
      "agent": [
        "codex",
        "copilot"
      ],
      "core": {
        "bundle_sha256": "0a5a8a5525678db3084f7e5443b10510769a754739c8a66a8ee1a62168216ca0",
        "files": {
          "kit/core/skills/issue/SKILL.md": "$SHA256"
        }
      },
      "loop": {
        "declined_at": "$NOW",
        "installed": false
      },
      "project": "demo",
      "source": "server",
      "url": "$API",
      "version": 1
    }
ws/.codex/config.toml (0644)
    # looptrack issue init が足した: AI が打つ looptrack issue にイシュー管理サーバの場所を渡す（サンドボックスのネットワークの許可ではない）
    [shell_environment_policy]
    set = { LOOPTRACK_API_URL = "$API", LOOPTRACK_PROJECT = "demo" }
ws/.codex/hooks.json (0644)
    {
      "hooks": {
        "PostToolUse": [
          {
            "hooks": [
              {
                "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent codex",
                "timeout": 10,
                "type": "command"
              }
            ],
            "matcher": "mcp__.*"
          }
        ],
        "SessionEnd": [
          {
            "hooks": [
              {
                "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent codex",
                "timeout": 10,
                "type": "command"
              }
            ]
          }
        ],
        "SessionStart": [
          {
            "hooks": [
              {
                "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook summary --agent codex",
                "timeout": 10,
                "type": "command"
              }
            ]
          }
        ],
        "Stop": [
          {
            "hooks": [
              {
                "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent codex",
                "timeout": 10,
                "type": "command"
              }
            ]
          }
        ]
      }
    }
ws/.github/hooks/looptrack.json (0644)
    {
      "hooks": {
        "PostToolUse": [
          {
            "bash": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event PostToolUse",
            "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event PostToolUse",
            "powershell": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event PostToolUse",
            "timeoutSec": 10,
            "type": "command",
            "windows": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event PostToolUse"
          }
        ],
        "SessionEnd": [
          {
            "bash": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event SessionEnd",
            "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event SessionEnd",
            "powershell": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event SessionEnd",
            "timeoutSec": 10,
            "type": "command",
            "windows": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event SessionEnd"
          }
        ],
        "SessionStart": [
          {
            "bash": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook summary --agent copilot",
            "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook summary --agent copilot",
            "powershell": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook summary --agent copilot",
            "timeoutSec": 10,
            "type": "command",
            "windows": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook summary --agent copilot"
          }
        ],
        "Stop": [
          {
            "bash": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event Stop",
            "command": "LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo looptrack hook usage --agent copilot --event Stop",
            "powershell": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event Stop",
            "timeoutSec": 10,
            "type": "command",
            "windows": "$env:LOOPTRACK_API_URL='$API'; $env:LOOPTRACK_PROJECT='demo'; looptrack hook usage --agent copilot --event Stop"
          }
        ]
      },
      "version": 1
    }
ws/AGENTS.md (0644)
    <!-- looptrack:begin（looptrack issue init が管理する節。直すときは looptrack issue init を再実行する） -->
    ## 課題管理（イシュー管理サーバ）

    **作業はイシュー登録から始める。会話だけで進めない。不具合は発覚と同時に起票する。**

    - **操作は MCP のツール（サーバ名 looptrack）で行う。** Codex のサンドボックスはシェルのコマンドの外部通信を止めるので、
      CLI（`looptrack issue`）は実行のたびに権限の昇格（利用者の承認）が要る。MCP のツールの通信はサンドボックスに止められない
    - **使い方とルールは最初に `guide` ツールで読む**（共通規則 + このプロジェクトのルール + 運用文書を 1 回で返す）
    - ループ運用の 1 周: `next`（着手）→ 作業 → `add_comment`（分かった時点で記録）→ 受け入れ条件を検証 → `set_status` で Done（`comment` に検証結果）→ 次の `next`。
      起票は `create_issue`、本文の編集は `get_issue` で version と全文を取り `update_issue` に直した全文を渡す。拒否されたらメッセージの指示に従う
    - 検証コマンド（本文の「## 検証コマンド」節）: `verify_issue` で一覧を取り、手元のシェルで順に全部実行して、結果を `report_verify` で送る
    - トークン情報: MCP で起票・コメント・状態変更をすると、フック（`.codex/hooks.json` の `looptrack hook usage`）が数秒後にイシューへ付ける。
      Done の前に `add_comment` で経過を残しておけば、規則 usage（AI の Done にトークン情報を求める）に拒否されない
    - CLI は MCP に無い操作だけに使う。権限を上げて実行し、利用者の承認を得る（環境変数 `LOOPTRACK_API_URL=$API` / `LOOPTRACK_PROJECT=demo` は init が `.codex/config.toml` に書いた。
      `looptrack issue config` がサーバの URL が無いというエラーで止まるなら、コマンドの前に `LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo` を付ける）:
      - `looptrack issue usage attach <ID>`: ツールの結果にこのコマンドが示されたとき（目印は文面ではなくコマンド。サーバの文面は利用者の言語で変わる）。`set_status` が規則 usage で拒否されたときは、
        数秒おいて 1 回やり直し（フックの送信が間に合っていないことがある）、それでも拒否されたら付けてからやり直す
      - `looptrack issue login --browser --url $API`: 初回だけ（フックが使うトークン。開いたブラウザで利用者がログインと承認をする。トークンは会話に出ない）
      - 導入・更新（`init`・`installed`）: `setup` ツールが返す手順どおり
    - MCP の looptrack が接続されていないときは、CLI の `next` / `comment` / `verify` / `close --comment` で同じ 1 周を回す（毎回承認が要る。
      利用者に `~/.codex/config.toml` への `[mcp_servers.looptrack]` の追加を勧める）。本文・コメントはクォート付きヒアドキュメント（`<<'EOF'`）で渡す
    - 閲覧: $API/p/demo/ （ログイン必須・閲覧専用）

    ### GitHub Copilot から使うとき

    - 上と同じく MCP のツールが主（VS Code は `.vscode/mcp.json`、Copilot CLI はリポジトリの `.github/mcp.json` か `~/.copilot/mcp-config.json` の looptrack）。Copilot にサンドボックスは無いが、
      CLI を打つときはコマンドの前に `LOOPTRACK_API_URL=$API LOOPTRACK_PROJECT=demo` を付ける（Copilot は `.codex/config.toml` を読まない）
    - トークン情報: 利用者が Copilot の OpenTelemetry のファイル出力を有効にすると、フック（`.github/hooks/looptrack.json` の `looptrack hook usage`）が付ける
      （手順は AI-GUIDE §7-3-1）。有効にしていなければ付かず、未付与にも数えない。
      Copilot CLI のシェルでは `looptrack issue usage attach <ID>` で付けられる（`COPILOT_AGENT_SESSION_ID` から会話を特定する）。
      **VS Code の Copilot では `looptrack issue usage attach` を実行しない**（シェルにセッション ID が渡らず、同じディレクトリの Codex の記録を付けてしまうことがある）
    <!-- looptrack:end -->
